# SANE Repository Setup

<!-- This ignored local file is created by SANE repository initialization. Keep
both absolute paths current when either repository moves. -->

- Implementation repository: `<absolute-path-to-implementation-repository>`
- Workstream repository: `<absolute-path-to-workstream-repository>`

<!-- The optional sibling current-workstream file contains one normalized,
workstream-relative selected path and is managed by the Alpha helper commands. -->
